# 🟢 Your Groq Key
groq.api.key=YOUR_API_KEY

# 🟢 Backend Port (Match your logs)
server.port=8081

# 🟢 MongoDB (Local)
spring.data.mongodb.uri=mongodb://localhost:27017/aitrip

# 🔴 DISABLE SQL (Prevents startup failure)
spring.datasource.url=
spring.jpa.hibernate.ddl-auto=none